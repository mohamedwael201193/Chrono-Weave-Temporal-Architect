import { type CountryCode, AsYouType, getCountryCallingCode } from 'libphonenumber-js/min';
export { type CountryCode, getCountryCallingCode };
/**
 * Do a lightweight validation of phone number. We'll do much heavier validation
 * on the backend, so this just needs to be sensible. This hints a default country
 * code of the US/+1, but will work for international numbers, provided they give
 * the country code in the number (ie +49 123123123).
 */
export declare const validatePhoneNumber: (phoneNumber: string, countryCode: CountryCode) => boolean;
export declare const formatPhoneNumber: (phoneNumber: string, countryCode: CountryCode) => string;
export declare const lastFourDigits: (phoneNumber: string) => string;
export declare const phoneNumberTypingFormatter: (countryCode: CountryCode) => AsYouType;
export declare const countryCodesAndNumbers: {
    code: CountryCode;
    callCode: import("libphonenumber-js/types").CountryCallingCode;
}[];
export declare const getPlaceholderPhoneNumber: (countryCode: CountryCode) => string | undefined;
export declare const getPhoneCountryCodeAndNumber: (phoneNumber: string) => {
    countryCode: CountryCode;
    phone: string;
};
